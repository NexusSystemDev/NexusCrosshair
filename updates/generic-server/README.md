# Generic Update Server

After `npm run dist`, publish these files from `dist/` to your update host:

- `latest.yml`
- `Nexus Crosshair Pro Setup.exe`
- `Nexus Crosshair Pro Setup.exe.blockmap`

Configured URL:

`https://updates.nexustools.local/nexus-crosshair-pro/`
